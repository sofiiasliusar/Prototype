<Screen>:
    canvas.before:
        Color:
            rgba: 0.941, 0.949, 0.973, 1  # Background color (RGB values)
        Rectangle:
            pos: self.pos
            size: self.size

<Label>:
    color: 0, 0, 0, 1 

<DescriptionInput@TextInput>:
    hint_text: "Description"
    multiline: False
    height: 50
    background_color: 0, 0, 0, 0  # Transparent background
    foreground_color: 0, 0, 0, 1  # Black text color
    padding: [5, 5, 5, 0]  # Padding, excluding bottom padding
    font_size: 16  # Font size

    canvas.before:
        Color:
            rgba: 0, 0, 0, 1  # Black color
        Rectangle:
            pos: self.pos[0], self.pos[1]
            size: self.size[0], 1
# <MySpinner@Spinner>:
#     height: 50  # Adjust the height of the spinner options as needed
#     padding: [10, 10]  # Adjust padding as needed
    
#     # Specify image paths as options
#     options: [{'text': '', 'background_normal': 'image1.png'}, 
#               {'text': '', 'background_normal': 'image2.png'}, 
#               {'text': '', 'background_normal': 'image3.png'}]